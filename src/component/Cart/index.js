import React from "react"

class Cart extends React.Component {
    render() {
        return (
            <fieldset>
                <legend>Cart</legend>
            </fieldset>
        )
    }
}

export default Cart;